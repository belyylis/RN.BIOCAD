import { Palette } from 'consts';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    fontSize: 24,
    marginBottom: 16,
    color: Palette.RED,
  },
});
